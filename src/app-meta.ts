/**
 * App identity in one place: the header, the landing card and the README all
 * read from here, so renaming the app is a one-line change.
 *
 * Merged from the cursor/gap-first-workspace-3cdc branch, which called the app
 * "Study Tree". The repository and the implementation brief call it CanvasQuest,
 * so that name stays until the product name is settled.
 */
export const APP_NAME = 'CanvasQuest';

export const APP_TAGLINE = 'An agent plants a learning tree, you grow it together, and it stands in your forest';

/** Board id for the game being played, as opposed to a tree planted in the forest. */
export const ACTIVE_BOARD_ID = 'active';
