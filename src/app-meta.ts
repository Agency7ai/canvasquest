/**
 * App identity in one place: the header, the planting panel and the README all
 * read from here, so renaming the app is a one-line change.
 *
 * The wordmark on the screen reads "Study Tree", the name the
 * cursor/gap-first-workspace-3cdc branch used. The repository, the storage keys,
 * the agent prompt and the terminal's nameplate still say CanvasQuest.
 */
export const APP_NAME = 'Study Tree';

export const APP_TAGLINE = 'An agent plants a learning tree, you grow it together, and it stands in your forest';

/** Board id for the game being played, as opposed to a tree planted in the forest. */
export const ACTIVE_BOARD_ID = 'active';
